package com.blake.math

object Definitions {
  object Types {
    type Factor = Long
    type Prime = Long
    type MathInteger = Long // members of the integers in math are not limited in size
  }

}
